package model.gameobjects;

public class BackgroundModel extends ScrollableModel {

	public BackgroundModel(float x, float y, int width, int height,
			float scrollSpeed) {
		super(x, y, width, height, scrollSpeed);
	}
}
